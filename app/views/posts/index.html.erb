<h2>Blog Spot</h2>
<% @posts.each do |post| %>
	<div class="well">
	<h3><%= post.title %></h3>
	<p><%= post.body %></p>
	<%= link_to "Explore", post_path(post), :class => 'btn btn-secondary' %>
	</div>
<% end %>
